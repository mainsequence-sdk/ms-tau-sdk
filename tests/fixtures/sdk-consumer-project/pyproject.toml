[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ms-tau-sdk-consumer-fixture"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "ms-tau-sdk==1.1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/project_fixture"]

[tool.uv.sources]
ms-tau-sdk = { path = "../../..", editable = false }
